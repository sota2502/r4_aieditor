<%= form_with(model: action_chain, local: true) do |form| %>
  <% if action_chain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(action_chain.errors.count, "error") %> prohibited this action_chain from being saved:</h2>

      <ul>
        <% action_chain.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
